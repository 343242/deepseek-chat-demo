target/
.env

# Dev environment config (contains real API keys)
src/main/resources/application-dev.yml
*.class
*.jar
*.log
.idea/
*.iml
.settings/
.project
.classpath
.DS_Store
graphify-out/
.omc/
.agents/
.claude/
.codex/
.trellis/worktrees/
.trellis/.cache/
logs/
CLAUDE.md
AGENTS.md
.code-review-graphignore
.factorypath

# Sandbox / mirror directory (local platform experimentation, not source)
bin/

# Local skill lock file (not source)
.impeccable
skills-lock.json

# Platform tooling (not source)
.omp/
lsp.json

# ZCode local workspace (not source)
.zcode/

# Production deployment artifacts (contains TLS private keys)
certbot/

# Frontend (frontend/) — 依赖与构建产物不入库，源码入库
frontend/node_modules/
frontend/dist/
frontend/.vite/

# Evaluation (eval/) — Python 虚拟环境不入库
eval/ragas-eval/
# 含真实 API key，不入库（模板见 config.example.json）
eval/config.json

