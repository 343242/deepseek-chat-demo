# Node.js 沙箱镜像
# 构建命令：docker build -f Dockerfile.node -t sandbox-node:bookworm .
# 需要先拉取基础镜像：docker pull node:22-bookworm
FROM node:22-bookworm

RUN useradd -m -s /bin/bash sandbox
USER sandbox
WORKDIR /tmp
