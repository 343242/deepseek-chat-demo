<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smart.rag.infrastructure.audit.mapper.AdminAuditLogMapper">

    <resultMap id="BaseResultMap" type="com.smart.rag.infrastructure.audit.entity.AdminAuditLog">
        <id column="id" property="id"/>
        <result column="operator_id" property="operatorId"/>
        <result column="operator_name" property="operatorName"/>
        <result column="operator_role" property="operatorRole"/>
        <result column="resource_type" property="resourceType"/>
        <result column="resource_id" property="resourceId"/>
        <result column="action" property="action"/>
        <result column="request_payload" property="requestPayload"/>
        <result column="result_status" property="resultStatus"/>
        <result column="error_code" property="errorCode"/>
        <result column="error_message" property="errorMessage"/>
        <result column="ip_address" property="ipAddress"/>
        <result column="user_agent" property="userAgent"/>
        <result column="duration_ms" property="durationMs"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, operator_id, operator_name, operator_role, resource_type, resource_id,
        action, request_payload, result_status, error_code, error_message,
        ip_address, user_agent, duration_ms, created_at
    </sql>

    <select id="selectByResource" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM admin_audit_log
        WHERE resource_type = #{resourceType}
        <if test="resourceId != null">
            AND resource_id = #{resourceId}
        </if>
        ORDER BY created_at DESC
        LIMIT #{limit}
    </select>

    <select id="selectByOperator" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM admin_audit_log
        WHERE operator_id = #{operatorId}
        ORDER BY created_at DESC
        LIMIT #{limit}
    </select>

</mapper>
