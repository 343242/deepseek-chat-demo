<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smart.rag.rag.mapper.EventMapper">

    <!-- 按文档 ID 删除事件（级联清理用；document_id 是 event 归属的权威记录，
         不依赖 vector_store 反查——fastTrack 临时行被删后仍能兜底） -->
    <delete id="deleteByDocumentId">
        DELETE FROM rag_event
        WHERE document_id = #{documentId}
    </delete>

    <!-- 按 chunk_ids 批量删除 -->
    <delete id="deleteByChunkIds">
        DELETE FROM rag_event
        WHERE chunk_id IN
        <foreach collection="chunkIds" item="cid" open="(" separator="," close=")">#{cid}::uuid</foreach>
    </delete>

    <!-- 插入（chunk_id 有唯一约束，冲突时忽略） -->
    <insert id="insertIgnore">
        INSERT INTO rag_event (chunk_id, summary, user_id, team_id, document_id)
        VALUES (#{event.chunkId}::uuid, #{event.summary}, #{event.userId}, #{event.teamId}, #{event.documentId})
        ON CONFLICT (chunk_id) DO NOTHING
    </insert>

    <!-- 多值批量插入（冲突忽略）；调用方按 500 行分批，自动提交单语句 -->
    <insert id="insertIgnoreBatch">
        INSERT INTO rag_event (chunk_id, summary, user_id, team_id, document_id)
        VALUES
        <foreach collection="events" item="event" separator=",">
            (#{event.chunkId}::uuid, #{event.summary}, #{event.userId}, #{event.teamId}, #{event.documentId})
        </foreach>
        ON CONFLICT (chunk_id) DO NOTHING
    </insert>

    <!-- 该文档已写事件的 chunkId（抽取增量过滤：rag_event 侧完成判定源） -->
    <select id="selectChunkIdsByDocumentId" resultType="string">
        SELECT chunk_id::text
        FROM rag_event
        WHERE document_id = #{documentId}
    </select>

    <!-- 孤儿事件清扫（§6 对账阶段一）：rag_event 自带 scope 列直接限定；逻辑删除行视同不存在。
         同步修复 SAG 检索层（expandChunks）的僵尸事件。 -->
    <delete id="deleteOrphanEventsByScope">
        DELETE FROM rag_event e
        WHERE e.user_id = #{userId,jdbcType=BIGINT}
          AND (#{teamId,jdbcType=BIGINT} IS NULL AND e.team_id IS NULL OR e.team_id = #{teamId,jdbcType=BIGINT})
          AND NOT EXISTS (SELECT 1 FROM rag_document d WHERE d.id = e.document_id AND d.deleted = 0)
    </delete>

    <!-- 孤儿事件探测（§6 阶段〇无锁只读）：谓词同 deleteOrphanEventsByScope，仅 EXISTS -->
    <select id="existsOrphanEventsByScope" resultType="java.lang.Boolean">
        SELECT EXISTS (
            SELECT 1
            FROM rag_event e
            WHERE e.user_id = #{userId,jdbcType=BIGINT}
              AND (#{teamId,jdbcType=BIGINT} IS NULL AND e.team_id IS NULL OR e.team_id = #{teamId,jdbcType=BIGINT})
              AND NOT EXISTS (SELECT 1 FROM rag_document d WHERE d.id = e.document_id AND d.deleted = 0)
        )
    </select>

</mapper>
