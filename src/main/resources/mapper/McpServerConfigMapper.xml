<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smart.rag.mcp.admin.mapper.McpServerConfigMapper">

    <resultMap id="BaseResultMap" type="com.smart.rag.mcp.admin.entity.McpServerConfig">
        <id column="id" property="id"/>
        <result column="server_id" property="serverId"/>
        <result column="remote_server_name" property="remoteServerName"/>
        <result column="url" property="url"/>
        <result column="name" property="name"/>
        <result column="description" property="description"/>
        <result column="enabled" property="enabled"/>
        <result column="auto_connect" property="autoConnect"/>
        <result column="bearer_token_encrypted" property="bearerTokenEncrypted"/>
        <result column="desired_state_hash" property="desiredStateHash"/>
        <result column="observed_state_hash" property="observedStateHash"/>
        <result column="catalog_synced" property="catalogSynced"/>
        <result column="error_code" property="errorCode"/>
        <result column="error_message" property="errorMessage"/>
        <result column="consecutive_failures" property="consecutiveFailures"/>
        <result column="next_reconcile_at" property="nextReconcileAt"/>
        <result column="last_attempt_at" property="lastAttemptAt"/>
        <result column="last_connected_at" property="lastConnectedAt"/>
        <result column="create_request_key" property="createRequestKey"/>
        <result column="version" property="version"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, server_id, remote_server_name, url, name, description, enabled, auto_connect,
        bearer_token_encrypted, desired_state_hash, observed_state_hash, catalog_synced,
        error_code, error_message, consecutive_failures, next_reconcile_at, last_attempt_at,
        last_connected_at, create_request_key, version, created_at, updated_at
    </sql>

    <select id="selectByServerId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM mcp_server_config
        WHERE server_id = #{serverId}
    </select>

    <select id="selectByCreateRequestKey" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM mcp_server_config
        WHERE create_request_key = #{createRequestKey}
    </select>

    <select id="selectAutoConnectEnabled" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM mcp_server_config
        WHERE enabled = TRUE AND auto_connect = TRUE
    </select>

    <!-- Recovery scan: enabled rows with due time -->
    <select id="selectDueForReconcile" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM mcp_server_config
        WHERE enabled = TRUE
          AND next_reconcile_at IS NOT NULL
          AND next_reconcile_at &lt;= CURRENT_TIMESTAMP
        ORDER BY next_reconcile_at
        LIMIT #{limit}
    </select>

    <!-- Conditional observed success: only commits if hash still matches -->
    <update id="updateObservedSuccess">
        UPDATE mcp_server_config
        SET observed_state_hash = #{observedStateHash},
            catalog_synced = FALSE,
            remote_server_name = #{remoteServerName},
            last_connected_at = CURRENT_TIMESTAMP,
            error_code = NULL,
            error_message = NULL,
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
          AND enabled = TRUE
          AND desired_state_hash = #{capturedHash}
    </update>

    <!-- Conditional observed failure: persists safe error + retry scheduling -->
    <update id="updateObservedFailure">
        UPDATE mcp_server_config
        SET error_code = #{errorCode},
            error_message = #{errorMessage},
            consecutive_failures = #{consecutiveFailures},
            next_reconcile_at = #{nextReconcileAt},
            last_attempt_at = CURRENT_TIMESTAMP,
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
          AND enabled = TRUE
          AND desired_state_hash = #{capturedHash}
    </update>

    <update id="updateBearerToken">
        UPDATE mcp_server_config
        SET bearer_token_encrypted = #{bearerTokenEncrypted},
            version = version + 1,
            updated_at = NOW()
        WHERE server_id = #{serverId} AND version = #{version}
    </update>

    <update id="updateEnabled">
        UPDATE mcp_server_config
        SET enabled = #{enabled},
            version = version + 1,
            updated_at = NOW()
        WHERE server_id = #{serverId}
    </update>

    <!-- Mark catalog as synced for the currently observed desired state -->
    <update id="markCatalogSynced">
        UPDATE mcp_server_config
        SET catalog_synced = TRUE,
            error_code = NULL,
            error_message = NULL,
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
          AND desired_state_hash = #{capturedHash}
          AND observed_state_hash = #{capturedHash}
    </update>

    <!-- Clear observation + schedule reconcile (reconnect / enable) -->
    <update id="clearObservation">
        UPDATE mcp_server_config
        SET observed_state_hash = NULL,
            catalog_synced = FALSE,
            error_code = NULL,
            error_message = NULL,
            consecutive_failures = 0,
            next_reconcile_at = CURRENT_TIMESTAMP,
            version = version + 1,
            updated_at = NOW()
        WHERE server_id = #{serverId} AND version = #{version}
    </update>

    <!-- Clear catalog_synced only + schedule reconcile (refresh-tools) -->
    <update id="clearCatalogSynced">
        UPDATE mcp_server_config
        SET catalog_synced = FALSE,
            next_reconcile_at = CURRENT_TIMESTAMP,
            version = version + 1,
            updated_at = NOW()
        WHERE server_id = #{serverId} AND version = #{version}
    </update>

    <!-- Update URL + recompute desired hash + clear observation -->
    <update id="updateDesiredUrl">
        UPDATE mcp_server_config
        SET url = #{url},
            desired_state_hash = #{desiredStateHash},
            observed_state_hash = NULL,
            catalog_synced = FALSE,
            error_code = NULL,
            error_message = NULL,
            consecutive_failures = 0,
            next_reconcile_at = CURRENT_TIMESTAMP,
            version = version + 1,
            updated_at = NOW()
        WHERE server_id = #{serverId} AND version = #{version}
    </update>

    <!-- Update bearer token + recompute desired hash + clear observation -->
    <update id="updateDesiredToken">
        UPDATE mcp_server_config
        SET bearer_token_encrypted = #{bearerTokenEncrypted},
            desired_state_hash = #{desiredStateHash},
            observed_state_hash = NULL,
            catalog_synced = FALSE,
            error_code = NULL,
            error_message = NULL,
            consecutive_failures = 0,
            next_reconcile_at = CURRENT_TIMESTAMP,
            version = version + 1,
            updated_at = NOW()
        WHERE server_id = #{serverId} AND version = #{version}
    </update>

    <!-- Enable + clear observation + schedule reconcile -->
    <update id="enableAndScheduleReconcile">
        UPDATE mcp_server_config
        SET enabled = TRUE,
            observed_state_hash = NULL,
            catalog_synced = FALSE,
            error_code = NULL,
            error_message = NULL,
            consecutive_failures = 0,
            next_reconcile_at = CURRENT_TIMESTAMP,
            version = version + 1,
            updated_at = NOW()
        WHERE server_id = #{serverId} AND version = #{version}
    </update>

    <!-- Disable + clear observation + clear reconcile schedule -->
    <update id="disableAndClearReconcile">
        UPDATE mcp_server_config
        SET enabled = FALSE,
            observed_state_hash = NULL,
            catalog_synced = FALSE,
            error_code = NULL,
            error_message = NULL,
            consecutive_failures = 0,
            next_reconcile_at = NULL,
            version = version + 1,
            updated_at = NOW()
        WHERE server_id = #{serverId} AND version = #{version}
    </update>

</mapper>
