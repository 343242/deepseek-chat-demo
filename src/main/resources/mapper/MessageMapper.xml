<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smart.rag.conversation.mapper.MessageMapper">

    <select id="selectAllByConversationId" resultType="com.smart.rag.conversation.entity.Message">
        SELECT id, conversation_id, parent_id, role, content, status, model_id, thinking_enabled, token_usage, duration_ms, created_at, updated_at FROM message WHERE conversation_id = #{conversationId} ORDER BY created_at ASC
    </select>

    <select id="selectRootMessages" resultType="com.smart.rag.conversation.entity.Message">
        SELECT id, conversation_id, parent_id, role, content, status, model_id, thinking_enabled, token_usage, duration_ms, created_at, updated_at FROM message WHERE conversation_id = #{conversationId}
        AND parent_id IS NULL ORDER BY created_at ASC
    </select>

    <select id="selectChildren" resultType="com.smart.rag.conversation.entity.Message">
        SELECT id, conversation_id, parent_id, role, content, status, model_id, thinking_enabled, token_usage, duration_ms, created_at, updated_at FROM message WHERE parent_id = #{parentId}
        AND conversation_id = #{conversationId} ORDER BY created_at ASC
    </select>

    <select id="selectLatestAssistant" resultType="com.smart.rag.conversation.entity.Message">
        SELECT id, conversation_id, parent_id, role, content, status, model_id, thinking_enabled, token_usage, duration_ms, created_at, updated_at FROM message WHERE conversation_id = #{conversationId}
        AND role = 'ASSISTANT' ORDER BY created_at DESC LIMIT 1
    </select>

    <select id="countByConversationId" resultType="int">
        SELECT COUNT(*) FROM message WHERE conversation_id = #{conversationId}
    </select>

</mapper>
