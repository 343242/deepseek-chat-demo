<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smart.rag.rag.mapper.RagDocumentMapper">

    <select id="selectFileSizeSum" resultType="java.lang.Long">
        SELECT COALESCE(SUM(file_size), 0) FROM rag_document
        WHERE team_id = #{teamId} AND user_id = #{userId} AND status != #{excludedStatus}
    </select>

    <update id="updateGroupId">
        UPDATE rag_document SET document_group_id = #{groupId} WHERE id = #{id}
    </update>

    <update id="updateGroupIdCas">
        UPDATE rag_document SET document_group_id = #{groupId} WHERE id = #{id} AND document_group_id IS NULL
    </update>

    <update id="updateSupersededByOnly">
        UPDATE rag_document SET superseded_by = #{newDocId} WHERE id = #{oldDocId} AND superseded_by IS NULL
    </update>

    <update id="updateGroupIdAndVersion">
        UPDATE rag_document SET document_group_id = #{groupId}, version = #{version} WHERE id = #{id}
    </update>

    <update id="updateSuperseded">
        UPDATE rag_document SET status = 'SUPERSEDED', superseded_by = #{newDocId} WHERE id = #{oldDocId} AND superseded_by IS NULL
    </update>

    <select id="findStaleSupersededTargets" resultType="com.smart.rag.rag.entity.RagDocument">
        SELECT * FROM rag_document WHERE superseded_by IS NOT NULL AND status != 'SUPERSEDED' AND deleted = 0
    </select>

</mapper>
