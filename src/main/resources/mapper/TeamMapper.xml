<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smart.rag.team.mapper.TeamMapper">

    <select id="selectByIdForUpdate" resultType="com.smart.rag.team.entity.Team">
        SELECT id, team_name, team_desc, creator_id, default_upload_limit_mb, creator_upload_limit_mb, status, created_at, updated_at, deleted FROM team WHERE id = #{id} FOR UPDATE
    </select>

</mapper>
